<center>
<h1 id='welcome'>PEPE PICKER</h1>

<%= form_for @game do |f| %>
<button type="submit" class="big">NEW GAME</button>
<% end %>

<h1>LEADERBOARD</h1>
<!-- <div class="leaderboard">
  <ul>
    <li>Name</li>
    <li>Turns</li>
  </ul>
  <ol><% @games.each do |game| %>
    <% if game.score >= 100 %>
    <li><%= game.player %> <%= game.turns %></li>
  <% end %>
  <% end %>
  </ol>

</div> -->


<table class="leaderboard">
  <tr>
    <th>Player</th>
    <th>Score</th>
  </tr>
  <% @games.each do |game| %>
  <% if game.turns >= 20 %>
  <tr>
    <td><%= game.player %></td>
    <td><%= game.score %></td>
  </tr>
  <% end %>
    <% end %>
</table>
